---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-portfolio-c824f56d                  # Ingress name
  labels:
    app: my-portfolio-c824f56d
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "my-portfolio-c824f56d.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-portfolio-c824f56d               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
